<i>It is common to use tuples as keys in dictionaries (primarily because
you can’t use lists).</i> For example, a telephone directory might map
from last-name, first-name pairs to telephone numbers.